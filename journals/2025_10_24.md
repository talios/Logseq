- 09:47 GP Appointment for Medication Review
- Turns out the #helidon `directory()` configuration option looks for files with named with the actual configuration keys - such as `app.name` rather than similar to `environmentVariables()` like `APP_NAME`, this is why secrets weren't getting mapped correctly for #messageremediation in #smx
	- I was also missing a template for